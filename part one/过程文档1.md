# 📄 第一部分过程文档

> 🖥️ **使用AI模型**: Gemini3 pro-preview

## 📚 题目M1

### 🔧 任务1：矢量微积分

**⚙️ 操作流程:**

#### 🗣️ AI 对话
> 你是mathematica高手。现在要完成一项mma项目。要求合理调用mma自带函数库，使完成步骤尽量简单有效，同时具有一定必要的注释。要求每一段落步骤代码分开给我，同时对于涉及到物理意义的步骤，在给我代码后再给我一段简短的物理意义解释。有什么不明白的需要提前问出，避免浪费资源。同时，关于作业中提到的物理意义，这里用ai完成简单的理解。
> （具体任务采用粘贴图片的方式给出）

*💡 思路：给ai提供背景，在mma中代码分段交付利于检查问题以及实时查看完成情况。最后给出可提问提示，减少不必要的token消耗*

#### 🤖 AI 回复
> 没有多余问题，交付对应代码

✅ **结果**: 代码正确无误

### 📝 物理意义解释

1.  **叉积 (Cross Product)**: 生成一个新的矢量，垂直于A和B所在的平面。
2.  **旋度 (Curl)**: 描述了矢量场的旋转特性或涡旋强度。计算结果说明场A具有旋转性（例如流体中的涡流）。
3.  **拉普拉斯算子叉乘等于0的结果**: 说明梯度场是无旋场。在物理上，这意味着保守力场（如静电场、重力场，它们是势函数的梯度）的旋度处处为零，沿闭合路径做功为零。
4.  **计算结果**: 是一个平方反比律的场，从原点向外辐射。

*🧐 物理解释*: 这个场与点电荷产生的静电场（库仑定律）或质点产生的引力场（万有引力定律）相似。虽然在 $r\neq0$ 处散度为 0，但在原点处存在“源”，数学上对应狄拉克函数。

---

### 🔧 任务2：静电场的计算

**⚙️ 操作流程:**

#### 🗣️ AI 对话
> 同上

✅ **结果1**: 第一部分代码报错

#### 🗣️ 修正步骤
> 复制报错信息，提交ai

*💡 原因*: 问题出在 `ContourPlot` 和 `StreamPlot` 中，使用了 `MainTitle` 这个选项，但正确的选项应该是 `PlotLabel`。`MainTitle` 在一些旧版本或特定包中可能存在，但在现代的 Mathematica 中，`PlotLabel` 是标准的标题选项。错误信息中的未知选项 `MainTitle` 和无法合并正是因此产生的。因为 `plotContour` 和 `plotStream` 由于选项错误未能成功生成图形对象，所以 `Show` 函数无法将它们合并。

#### 🤖 AI 回复
> **修正说明**：
> 1. 我移除了 `ContourPlot` 和 `StreamPlot` 中的 `MainTitle` 选项。
> 2. 在最后的 `Show` 函数中，使用 `PlotLabel` 来为合并后的图形设置一个总标题，这样更规范。
> 3. 我还添加了 `FrameLabel` 来标记 x 和 y 轴，使图像信息更完整。

✅ **结果**: 修正成功

### 📝 物理意义解释

1.  **电势叠加**: 电势是标量，直接相加即可。
2.  **梯度关系**: 电场线总是垂直于等势面，指向电势降低最快的方向（这就是负号的意义）。
3.  **图像解读**: 在 z=0 平面上，看到 (1,0) 和 (-1,0) 处有两个正电荷的“山峰”。虽然负电荷在 (0,0,1)（平面上方），但它会拉低原点附近的电势，使得场线在平面中心呈现出汇聚或鞍点的形态。

*🧐 讨论*: 因为特征值之和为零（且不全为零），所以必然存在正的特征值和负的特征值。这意味着平衡点在某些方向上是势能极小值（稳定），在另一些方向是极大值（不稳定）。这被称为鞍点。所以这些平衡点是不稳定的。如果你在这里放置一个试探电荷，它受到微小扰动后会沿某个方向滑落，无法保持静止。

---

## 📚 题目M2

### 🔧 任务1：阻尼振动的完整分析

**⚙️ 操作流程:**

#### 🗣️ AI 对话
> （同上，在同一话题下，使用图片提交任务）

#### 🤖 AI 回复
> （*结果：代码完整无误）

### 📊 可视化比较

*   **x(t)图**: 蓝色曲线（欠阻尼）穿越了 x=0 轴（平衡位置），表现出振荡；红色（临界）和绿色（过阻尼）没有穿越 x=0 轴，而是单调逼近。红色比绿色归零得更快。
*   **相图**:
    *   **欠阻尼**: 呈螺旋状（Spiral Sink），轨迹围绕原点旋转并收缩，代表振荡衰减。
    *   **过/临界阻尼**: 直接趋向原点（Nodal Sink），不围绕原点旋转，代表没有往复运动。

---

### 🔧 任务2：简单受迫振动

**⚙️ 操作流程:**

#### 🗣️ AI 对话
> （同上，在同一话题下，使用图片提交任务）

#### 🤖 AI 回复
> （*结果：代码完整无误）

### 🧐 观察与分析

*   **过渡过程**: 在 t 较小时（约0-20秒），图像有些杂乱，这是因为瞬态解（按自然频率衰减振荡）和稳态解（按驱动频率等幅振荡）叠加在一起，产生了类似“拍”的现象。
*   **稳态**: 随着时间推移，瞬态分量因为阻尼项而衰减消失，只剩下稳态分量。
*   **对比**: 红色曲线 `omega=1.0` 处于共振状态，发现它的振幅随着时间逐渐建立，最终达到一个很大的稳定值，远高于 `omega=0.5` 和 `omega=1.5` 的情况。
